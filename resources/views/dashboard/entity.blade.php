@extends('layouts.app-page')

@section('content')

    @if ($entity == 'categorie')
    @endif

    @if ($entity == 'plat')
    @endif

    @if ($entity == 'boisson')
    @endif

    @if ($entity == 'orders')
    @endif

@endsection